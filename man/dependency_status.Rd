% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dependencies.R
\name{dependency_status}
\alias{dependency_status}
\title{Inspect dependencies for a report profile}
\usage{
dependency_status(
  profile = c("full", "core", "report_only"),
  modules = NULL,
  species = "auto",
  input = NULL,
  render = TRUE
)
}
\arguments{
\item{profile}{One of `"full"`, `"core"`, or `"report_only"`.}

\item{modules}{Optional module selection in the same formats accepted by
[report_config()].}

\item{species}{One or more supported species, `"auto"`, or `"all"`.
Auto detection needs `input`; unresolved species are never guessed.}

\item{input}{Optional RDS path used only to detect the species when
`species = "auto"`.}

\item{render}{Whether the dependency plan must include HTML rendering.}
}
\value{
A data frame with installation, namespace-load, version, and system
  readiness information.
}
\description{
`dependency_status()` checks both package presence and whether each package
namespace can actually load. It also treats the Quarto CLI, JAGS, and the
HDF5 build prerequisite as external system dependencies rather than
confusing them with similarly named R packages.
}
