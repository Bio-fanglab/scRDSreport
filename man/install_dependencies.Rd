% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dependencies.R
\name{install_dependencies}
\alias{install_dependencies}
\title{Install dependencies for a report profile}
\usage{
install_dependencies(
  profile = c("full", "core", "report_only"),
  modules = NULL,
  species = "auto",
  input = NULL,
  render = TRUE,
  upgrade = FALSE,
  lib = NULL,
  dry_run = FALSE,
  strict = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{profile}{One of `"full"`, `"core"`, or `"report_only"`.}

\item{modules}{Optional module selection in the same formats accepted by
[report_config()].}

\item{species}{One or more supported species, `"auto"`, or `"all"`.
Auto detection needs `input`; unresolved species are never guessed.}

\item{input}{Optional RDS path used only to detect the species when
`species = "auto"`.}

\item{render}{Whether the dependency plan must include HTML rendering.}

\item{upgrade}{Whether to update already installed packages. The default is
`FALSE` to avoid changing unrelated working environments.}

\item{lib}{R library in which missing packages should be installed.
`NULL` selects the first writable active library, then falls back to the
configured user library.}

\item{dry_run}{Return the installation plan without changing the library.}

\item{strict}{Stop after installation if any required package, system
dependency, or species selection is still unresolved.}

\item{verbose}{Show installer progress.}
}
\value{
Invisibly, the final dependency status; with `dry_run = TRUE`, the
  returned data frame has an additional `action` column.
}
\description{
The installer uses Bioconductor repositories for CRAN/Bioconductor packages,
installs declared GitHub-only analysis engines separately, and then reruns
namespace-load and version checks. External programs such as Quarto, JAGS,
and HDF5 development libraries are reported with actionable instructions but
are not silently installed by an R package.
}
